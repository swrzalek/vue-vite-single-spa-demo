# Single Spa Demo with Vue and Vite

## Root App [Root]

Running the root app:

```bash
cd root
npm install
npm run dev
```

## Remote App [App]

```bash
cd root
npm install
npm run dev
```

Running the remote app as a standalone app:

```bash
echo "VITE_MODE=dev" > "app/.env"
```
